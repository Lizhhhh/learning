/**
 * 管理本地安装的npm包的最佳方法是创建一个package.json文件。
 *
 * 一个package.json文件 :
 *   1.列出项目所依赖的包
 *   2.允许您使用语义版本控制规则指定项目可以使用的包的版本
 *   3.使您的构建可重现,因此更容易与其他开发人员共享
 *
 * 一个package.json必须具备:
 *   1. "name"
 *     -- 全是小写
 *     -- 一个字,没有空格
 *     -- 破折号和下划线允许
 *   2. "version"
 *     -- 以...的形式 x.x.x
 *
 * 一个简单的.npm-init.js
 */

